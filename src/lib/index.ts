import { Environment } from "./Environment";
import { Danbooru } from "./Danbooru";

export const danbooruFactory = (
  env: Environment,
  server?: string,
  login?: [string, string]
): Danbooru => {
  const danbooru = () => {};

  return;
};
